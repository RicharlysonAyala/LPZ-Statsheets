# Copie este arquivo para ".env" e ajuste os valores.
# Em desenvolvimento local, pode deixar em branco (usa SQLite automaticamente).
# Em produção, cole aqui a connection string do Postgres (Neon, Supabase, Railway, etc).
DATABASE_URL=
